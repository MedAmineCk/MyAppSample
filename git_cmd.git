git init
git add
git status
git commit
git push
git pull
git clone
//*start*//

git init

git config --global user.name 'user name'
git config --global user.email 'email@gmai.com'

git add filename.extention
git add .
git add *.extention

git status

git commit -m 'type her a comment describe what you did'

.gitignore ==> name of the file o directory you want to ignore
.gitignore ==> file.txt /dir2 #will ignore the two files together


